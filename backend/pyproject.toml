[project]
name = "ai-suite-platform-backend"
version = "0.1.0"
description = "AI Suite Platform - FastAPI backend with chat and recipe-ready integrations"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.128.1",
    "openai>=1.30.0",
    "pydantic>=2.0.0"
]
